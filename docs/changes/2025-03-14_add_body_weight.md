# 仕様変更：体重記録機能の追加

## 背景
筋トレ記録と合わせて体重推移を管理したい

## 変更内容
1. DBにbody_weightテーブルを追加
2. 体重登録・取得APIを追加（/api/body-weight）
3. 既存の記録一覧画面に体重グラフを追加

## 制約
- 既存の筋トレ記録APIに影響を与えないこと
- 日付は既存と同じ形式（YYYY-MM-DD）を使うこと

## 優先順位
1. DB・API（必須）
2. グラフ表示（余裕があれば）
